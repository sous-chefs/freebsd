driver:
  name: vagrant

provisioner:
  name: chef_zero
  deprecations_as_errors: true
  chef_license: accept-no-persist

verifier:
  name: inspec

platforms:
- name: freebsd-10
- name: freebsd-11

suites:
- name: default
  run_list:
    - recipe[freebsd::portsnap]
    - recipe[test::default]
